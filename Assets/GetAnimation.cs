using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GetAnimation : MonoBehaviour
{
    public RuntimeAnimatorController mCharAnimator;
    Avatar mCharAvatar;

    Animator mAnimator;
    Avatar mAvater;
    // Start is called before the first frame update
    void Start()
    {
        //if(mCharAnimator!=null)
            //mCharAvatar = mCharAvatar.
       
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
